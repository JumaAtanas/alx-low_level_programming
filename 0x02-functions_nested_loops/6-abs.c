#include "main.h"
/**
 * _abs - compute 
 * the absolute value of an integer
 * @n: int type number
 * Return: absolute value of @n
 */
int _abs(int n)
int main()
{
	if (n <= 0)
	{
		return (i);
	}
	else
	{
		return i*-1);
	}
}
