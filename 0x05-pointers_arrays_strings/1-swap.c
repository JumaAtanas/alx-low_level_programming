#include <stdio.h>

/**
 * swap_int - Swwap the values of the two integers
 *a@the first int to be swapped
 @b the second it to be swappe
 *
 * Return: nothing
 */
void swap_int(int *a, int *b)
{
	temp = *a;
	*a = *b;
	*b = temp;
}
